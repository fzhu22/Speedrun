# Reject Log — create-actions (MCAT Anki Learning-Science Features)

Every implementation fact / step / approach cut at a gate, with the concrete reason. Format: `- <item> — REJECTED (<stage/test>): <specific reason>`.

## Implementation facts cut by the verification gate (Step 1b)
**0 facts hallucinated; substrate + legal all VERIFIED.** 3 dropped (cannot verify → not load-bearing) + 6 FIXes applied (in `01b-fact-verification.md`):
- Jack Westin "no public developer API" — REJECTED (Step 1b): no primary source affirms/denies → A11 integration is a BD/licensing question, no step may assume an API exists or doesn't.
- Khan/AAMC CARS-passage REUSE license for an A11 build — REJECTED (Step 1b): "free to use for practice" ≠ a redistribution/derivative license → any A11 passage reuse must verify the license first.
- anki-sync-server "100 MB default cap" — REJECTED (Step 1b): not found in the official doc (tunable/non-load-bearing).
- FIXes (not drops): SchedulingContext now 4 fields post-#4880 (DR/decay READable; WRITE still workaround); CCPA threshold $26.625M; Haiku 4.5 $1/$5; AGPL binding holder = Ankitects core; cite AnkiConnect at sourcehut + add-on `fsrs4anki-helper`.

## Steps / approaches cut or demoted during the critique round (Step 3)
Four isolated cross-family critics (action-skeptic, action-red-teamer, operator, finance/compliance) converged. No anti-pattern leaked and no step rested on a dropped fact; the cuts are scope/feasibility corrections, not vapor:
- **Mobile/iOS answer-time customData WRITE as the gated-state path (A3-S4, A6-S3, A7-S1, A9-S1, A4-S3)** — REJECTED as written (Step 3, all 4 critics + primary sources): no reliable synced review-tier write on iOS, Android-only via the tag channel; iOS write path is FATAL to immediate mobile unlock/confidence/remediation. → Re-scoped to **desktop-reconcile authoritative**; mobile = capture-local. (See 01b GAP-2/3 resolution.)
- **"Build AnkiDroid from source" inside the 3-day MVP** — REJECTED (Step 3, operator): the review tier runs on the STOCK app (AI-Hints precedent); building AnkiDroid (NDK+Gradle+rslib-bridge submodule) is a multi-hour toolchain task off the MVP critical path.
- **"Hire MCAT tutors (Wyzant)" as an MVP content source (A4-S1)** — REJECTED for MVP (Step 3, operator + finance): find/contract/NDA/brief a tutor is a week+. MVP content = in-house expert from hour one; hiring is a post-MVP scaling lever.
- **"CI-reproducible build" as a Wednesday acceptance criterion (A1-S1)** — REJECTED for MVP (Step 3, operator + red-teamer): standing up reproducible Rust+wheels+TS CI is a day+ on its own; MVP bar = "renamed fork launches + opens a collection" (build on WSL/Linux/Mac, clone to a **space-free path** — the workspace path has a space and will break the build).
- **APKG import/export as a "two devices converge" persistence story (A1-S5c)** — DEMOTED (Step 3, red-teamer + operator): APKG doesn't cleanly preserve scheduling/customData and can duplicate on GUID collision → relabel "portable demo export/import," not conflict-safe convergence.
- **A8-S2b native per-card-DR write as a "2–4 day" / Wednesday item** — DEMOTED to a separate engineering spike (Step 3, all): realistic ≈ 1–2 weeks with schema/protobuf/migration/sync/scheduler-consume + an FSRS-integrity regression test; AND it forces the schema-vs-stock-client fork (a schema field kills stock-client sync). MVP real-Rust-change = the A1-S1 `extract_custom_data` search (low-risk, testable); S2b is the higher-value optional, and if taken must write to `customData` to preserve stock clients.
- **"Prepare tier" as a multi-tenant service (A1-S2)** — DEMOTED (Step 3, operator): AnkiConnect runs inside a live Qt GUI; one headless Qt-Anki per user does not scale → MVP = a local single-collection script (idempotent/resumable/paginated/backup-first/clients-quiesced); a real backend (rslib-direct) is post-MVP.
- **"$0 / cash-light pilot" framing** — CORRECTED (Step 3, finance): ~$0 holds ONLY for an APKG-only / no-LLM / no-sync / in-house-content hackathon; a credible pilot is **$5k–25k** (content authoring is the cost driver) + ops + an unpriced **legal-review quote gate** before shipping A1-sync / A2-ingestion / A10 / A11.

## Steps / approaches cut or corrected by the Feasibility Testing Protocol (Step 5)
- **DECOY — S-Q "on-device semantic queue re-ranking"** — REJECTED (all 3 tests): Test 1 = 1–2/10 (both families "obviously infeasible"); Test 2 = "NO BINDING CONSTRAINT (wish)"; Test 3 = INFEASIBLE vs primary source (custom-scheduling JS mutates the current card only — `ts/reviewer/answering.ts`; queue built backend-side — `rslib/src/scheduler/queue/mod.rs`; AnkiMobile has no review-screen synced write). Also reintroduces anti-pattern #2. **The decoy was caught → the gate tracks feasibility, not rhetoric.**
- **A1-S1's chosen "mandatory real Rust change" (`extract_custom_data` SQLite search/column)** — REJECTED as the genuine engine change (Test 3): it **already exists in mainline** (`rslib/src/storage/sqlite.rs` + `has-cd:`/`prop:cdn:` in `search/parser.rs`) → nearly a no-op, does not satisfy the project's "make a real Rust change" requirement. The fork build is still Ready; the genuine Rust change is re-opened as a **Needs-Decision** (lead candidate = A8-S2b's customData-targeting native per-card-DR write — real rslib work that preserves stock sync).
- **A9-S2 `psychometrics` Python package** — REJECTED (Test 1 Claude flag, Test 3): not a verifiable PyPI library → compute corrected point-biserial via **`girth` (MIT) + `scipy.stats`/a direct formula** instead.
- **A8 (exam-date DR ramp) shipping any step now** — BLOCKED (Test 2 FAIL on A8-S2a/S2b): the *write path* (A8-S2a) is feasible, but the **ramp curve + cap policy is GAP-5 (unspecified)** → no A8 step ships until a gap-filling pass sets the policy; A8-S2b (native write) kept only as RISKY behind its 6-step DoD + FSRS-integrity regression + the stock-sync-vs-own-infra decision gate.
*(No real MVP step was dropped — the rest cleared Test 1 pass + Test 2 real binding constraint + Test 3 holds/partly-supported; the content/efficacy items are PROXY-ONLY by design, resolving at the pilot/RCT, consistent with the BrainLift's domain ceiling.)*
