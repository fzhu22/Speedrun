# Execution Plan (draft) — Forking Anki for the MCAT (A1–A12)

**Roles:** R = Rust/build eng · P = Python/data eng · J = card-template/JS eng · C = MCAT content author/reviewer (tutor) · L = PM/legal/BD. **Costs as-of 2026-06.** Each step carries the 5 required fields + a compact `Binding/faithful:` line.

## Sequencing & critical path (read first)
- **A1 is the foundation and MUST precede the review tiers of A3/A5/A7/A8/A10** (custom-scheduling JS sees only `{deck_name, seed}` (+decay/DR read), runs at answer-time, re-weights one card, **cannot re-rank the queue** → all concept-aware sequencing is desktop-prepared; AnkiMobile = no add-ons, AnkiDroid = no Python).
- **Critical path (MVP):** A1-S1 → A1-S2 → A1-S3 → A1-S4 → {A2, A3} → A9-S1/S2. Long pole = content authoring (A3-S1 / A4-S1), runs parallel from day 1.
- **Real Rust change (project given):** minimal `rslib` change in A1-S1, or higher-value the native per-card-DR WRITE in **A8-S2(b)**.
- **3-day MVP (Wed/Fri/Sun):** Wed = A1-S1/S2/S3/S4 + review on desktop+AnkiDroid; Fri = A2 (full) + A3 (one set); Sun = A4 (1–2 sets) + A9-S1/S2 + demo. Defer: iOS (A1-S6), sync-server, A5, A6 full, A7, A8 full, A10 (LLM+DPA), A11 build/integrate, A12 beyond a private indicator.

## A1 — Two-tier foundation (Validated SPOV 4) — FOUNDATION
**A1-S1 — Fork & build Anki from source; rename; land one real `rslib` change. [MVP]** How: clone `ankitects/anki` to space-free path; Rustup (pinned) + uv + N2/Ninja + `just`; `./run` builds rslib+rsbridge (PyO3); `tools/build`→wheels. Real Rust change candidate: SQLite search/column over `extract_custom_data(card.data,'key')` in rslib (or defer bigger change to A8-S2b). Own name+logo (Anki trademarked; logo AGPL). Resources: $0 (AGPL); 1×R; 0.5–1 day. Deps: none. Accept: renamed build launches from source, opens a collection, CI-reproducible, rslib change has a test. Binding/faithful: toolchain must match pin; foundation for SPOV 4; no anti-pattern.
**A1-S2 — Desktop "prepare" tier. [MVP]** How: Python over `pylib/anki`; drive edits via AnkiConnect (`POST localhost:8765`: findNotes/notesInfo/addNote/updateNoteFields/updateNoteTags/setSpecificValueOfCard/changeDeck/setDueDate); cron/offline (AnkiAIUtils pattern). Gotchas: note open in Browser silently won't update; macOS keep foreground + disable App Nap. Resources: $0 (AnkiConnect GPLv3, `git.sr.ht/~foosoft/anki-connect`); 1×P; 1 day. Deps: A1-S1. Accept: prepare script writes field/tag/customData appearing on a 2nd device. Binding/faithful: write-backs survive sync; runs OFF the answer path (no live re-rank).
**A1-S3 — Cross-platform "review" tier runtime (card-template JS). [MVP]** How: template `<script>` JS only (proven cross-client by AI-Hints' unified UI script on AnkiDroid/AnkiMobile/AnkiWeb with no add-on); reserve `card_will_show`+`pycmd`+Python for desktop-only. Resources: $0; 1×J; 1–2 days. Deps: A1-S1. Accept: one note type renders an interactive card identically on desktop/AnkiDroid/AnkiMobile. Binding/faithful: mobile JS-only → anything needing Python moves to A1-S2.
**A1-S4 — Syncable-artifact channel (FORK — 3 approaches). [MVP: a+b]** (a) tiny `customData` flags (key "cd", ≤8-byte keys/≤100-byte total; `{"tt":1}`,`{"dr":92}`; query via extract_custom_data); (b) tag namespaces (`#confusable::inhibition::competitive`; AnKing `#AK_MCAT_v2::…`; syncs natively, dodges 100B; suspend-all/unsuspend-by-tag = opt-in gating); (c) companion store (AnkiHub-style) for big artifacts. Recommended (a)+(b) MVP; (c) later. Resources: (a)/(b) $0; (c) $6–15/mo; 1×R/P; 1 day. Deps: A1-S2. Accept: a >100-byte artifact (5-item sort set) round-trips desktop→mobile. Binding/faithful: never exceed validate_custom_data (silent truncation).
**A1-S5 — Sync path (FORK — 3) + schema migration. [MVP: c]** (a) self-host `anki-sync-server` (HTTP-only→add TLS; individual/family; multi-tenant auth/scaling = your burden); (b) request written Ankitects permission for AnkiWeb (do NOT budget AnkiWeb as a programmable backend); (c) APKG import/export only (no live sync) — hackathon-safe. Recommended (c) MVP, (a) pilot. Version the schema; sync-conflict under single-writer `with_col` lock. Resources: (a) DO $6–12/mo+ops; (b) $0+legal; (c) $0. Deps: A1-S2,S4. Accept: two devices converge on custom fields/tags/customData with no loss. Binding/faithful: AnkiWeb ToS forbids third-party clients w/o permission → (a)/(b) only.
**A1-S6 — iOS review-tier path (FORK — LEGAL — 3). [DEFER]** (a) review tier = card-template JS inside the EXISTING AnkiMobile (legal workaround: AGPL §7 vs Apple Usage Rules; VLC/GNU-Go pulled); (b) 100%-own thin client to a server (no AGPL engine on-device); (c) defer iOS, Android-first (Play permits AGPL; AnkiDroid Kotlin+JNI rslib-bridge). Recommended (a)+(c). NEVER ship an own engine-bearing iOS app. Resources: Apple $99/yr only if (b). Deps: A1-S3,S5. Accept: a fork card renders+interactive on AnkiMobile via template JS. Binding/faithful: §7 exception needs every holder → impossible → path (a).
**A1-S7 — AGPL packaging, §13 boundary, naming. [partial MVP: licensing]** How: publish modified source (AGPL-3.0-or-later); any modified-engine server (sync/prepare/LLM gateway) offers source per §13 OR sits behind a non-AGPL API boundary; own name/mark. Resources: 1×L ~0.5 day + [legal]. Deps: A1-S1. Accept: repo builds from published source; §13 compliant; own name/logo. Binding/faithful: §13 network-use.

## A2 — Student-authored miss→card scaffold + DISCONFIRMER (SPOV 1) — flagship [MVP]
**A2-S1 — "Miss→Card" note type w/ required DISCONFIRMER field. [MVP]** How: fields = Provenance/QID, Student's principle, Perturbed surface, Trap-flag, **Disconfirmer: what one fact flips this?**, Boundary-case; renders all clients via A1-S3. Resources: $0; 1×J; 0.5 day. Deps: A1-S3 (authored on desktop A1-S2). Accept: all fields save; disconfirmer shows on back across clients. Binding/faithful: student authors body (generation effect) — no AI auto-authoring.
**A2-S2 — Miss ingestion (FORK — 3). [MVP: a+b]** (a) QID tag-lookup (paste UWorld/AAMC QID → `uworld:NNNNN` tag → spawn scaffold; stores only student's OWN write-up); (b) manual paste/typing; (c) OCR of student's OWN handwritten notes (Mistral OCR $4/1k [$2 batch] or Google/AWS $1.50/1k). NEVER screenshots of UWorld/AAMC (ToS auto-closes). Recommended (a)+(b). Resources: (a)/(b) $0; (c) ~$0.45–1.20/300pp. Deps: A1-S2. Accept: a miss logged from a QID/paste in <30s; no copyrighted stem stored. Binding/faithful (legal): student-authored ONLY.
**A2-S3 — Surface-perturbation authoring scaffold (desktop-prepare). [MVP]** How: flow prompting principle→surface-variation→trap→disconfirmer; optionally surface an AI severe-test prompt (A10) as a thinking aid — AI never writes the body. Resources: $0 (+optional LLM); 1×J/P; 0.5–1 day. Deps: A2-S1. Accept: finished card has student-authored principle+disconfirmer; AI populated nothing. Binding/faithful: automation must not touch the body.
**A2-S4 — Disconfirmer validation (reject blank/answer-restating). [MVP: basic]** How: client heuristics — non-empty; not a restatement of the answer; optional embedding-similarity ceiling (OpenAI text-embedding-3-small $0.02/1M); student can override. Resources: ~cents; 1×J; 0.5 day. Deps: A2-S1. Accept: blank/restating disconfirmer rejected with a revise prompt. Binding/faithful: AI heuristic, not authoritative grader.

## A3 — Deep-structure SORT verification gate (SPOV 2) — needs A1 [MVP: thin]
**A3-S1 — Author principle-sharing families w/ surface×deep crossing. [MVP: 1 set]** How: Chi/Feltovich/Glaser 1981 construction — CROSS surface×deep (same principle/diff cover; same surface/diff principle); ~24 problems; "sort by how they'd be solved." Resources: 1×C ($40–75/hr; senior $150–250/hr); ~0.5–1.5 hr/item. Deps: concept families (A4 graph helps); start day 1. Accept: a held-out set where a correct sort can't be a surface match. Binding/faithful: crossing must hold; no auto-tagging.
**A3-S2 — Held-out split logic (desktop-prepare). [MVP]** How: Python splits each family studied vs held-out; sort uses only held-out; emit split as tag/companion. Resources: $0; 1×P; 0.5 day. Deps: A1-S2, A3-S1. Accept: sort items provably disjoint from studied.
**A3-S3 — Sort-task template (review-tier JS). [MVP]** How: template `<script>` drag/tap "which two share the principle?", cross-client (A1-S3). Resources: $0; 1×J; 1 day. Deps: A1-S3, A3-S2. Accept: student completes a sort on desktop+AnkiDroid+AnkiMobile.
**A3-S4 — "transfer-trained" status flag + gate. [MVP]** How: on pass set `customData {"tt":1}` (≤100B); READ native (extract_custom_data/PR #4880); WRITE via desktop reconcile (AnkiConnect) or per-state customData at next answer (**GAP-2** mobile write-back). Gate on sort pass, NOT perturbed accuracy. Resources: $0; 1×J/P; 0.5–1 day. Deps: A1-S3,S4, A3-S3. Accept: pass flips `tt`+unlocks; fail doesn't; flag syncs. Binding/faithful: gated on held-out sort, never accuracy/streaks.

## A4 — Curated discrimination-drill library (SPOV 3) — needs A1 [MVP: 1–2 sets]
**A4-S1 — Author curated confusable sets (FORK — build vs hire). [MVP: 1–2 sets]** How: sets (acids/bases vs buffers; competitive/non-comp/uncompetitive inhibition; SN1/SN2); each drill forces *why X-not-Y* (mechanism)+*what consequence differs* — not the label; volunteer hand-merge model (AnKing MileDown-style), NEVER auto-tagged. (a) in-house; (b) hire MCAT tutors (Wyzant $40–75/hr; senior $150–250/hr); (c) hybrid (recommended). Resources: **largest non-software cost** — 100 drills @0.5–1.5 hr ≈ **$4,000–18,750**; MVP = 1–2 sets. Deps: opt-in after baseline; start day 1. Accept: ≥1 peer-reviewed set w/ mechanism+consequence per drill. Binding/faithful: curated/human-reviewed, science-only, never auto-tag.
**A4-S2 — Concept-graph as a tag namespace. [MVP]** How: tag namespace (`#confusable::inhibition::competitive`); suspend-all/unsuspend-by-tag = opt-in; bigger metadata → companion store. Resources: $0; 1×P; 0.5 day. Deps: A1-S4. Accept: drills addressable by graph tags; unsuspend-by-tag gates them.
**A4-S3 — Drill generator + free-response mechanism/consequence capture. [MVP: thin]** How: desktop-prepare generator emits drills; review template captures free-response; light keyword/embedding match to the human key (not authoritative AI grading). Resources: cents; 1×J/P; 1 day. Deps: A1-S2,S3, A4-S1,S2. Accept: student gets a drill, names mechanism, gets source-backed feedback; never final-auto-graded. Binding/faithful: human key decides correctness.

## A5 — Support-fading difficulty engine (SPOV 10) — needs A1 [DEFER]
**A5-S1 — Per-family expertise estimator (desktop-prepare).** How: count per-family unaided successes+transfer passes; fade only after ≥2 unaided + 1 transfer item; store rung state desktop-prepared (tags/companion; ≤100B flag). (**GAP-4:** estimator formula — mastery threshold vs py-irt Bayesian). Resources: $0 (py-irt MIT); 1×P; 1 day. Deps: A1-S2. Accept: advances only on threshold; reports per-family rung.
**A5-S2 — Author backward-fading worked examples + principle prompts.** How: Renkl & Atkinson backward fading — full → completion (last step omitted) → last two omitted → full; principle-naming prompt each rung; same deep structure/diff surface (Chi crossing); fade toward explaining the rejected alternative. Resources: 1×C; 0.5–1.5 hr/item. Deps: A4-S1 style. Accept: a family has a 4-rung ladder w/ principle prompts.
**A5-S3 — Fade-ladder state machine + incremental-reveal template.** How: front-side incremental reveal = template `<script>`+CSS via Anki-Cloze-Interactive `data-cloze`/Closet (native clozes HTML-converted → use data attrs); rung selection desktop-prepared (A5-S1); phase-exit rapid test advances. Resources: $0; 1×J; 1–2 days. Deps: A1-S3, A5-S1,S2. Accept: student moves worked-example→completion→open across sessions; high-element only; declarative untouched. Binding/faithful: per-family fading NOT global hard-mode; never declarative recall.

## A6 — MC feedback + error-classification engine (SPOV 6) — needs A1 [DEFER; thin=stretch]
**A6-S1 — Commit-then-reveal MC/application card type.** How: template `<script>` MC (cross-client per AI-Hints); distractor candidates may be AI-suggested but MUST be human-reviewed/source-backed. Resources: $0; 1×J; 1 day. Deps: A1-S3. Accept: card commits then reveals per-distractor rationale on all clients.
**A6-S2 — Source-backed distractor rationales + per-distractor QA.** How: author "why the lure tempts/what false model/what drill follows"; per-distractor point-biserial (+key, −distractors) via girth/psychometrics. Resources: 1×C; $0 psychometrics. Deps: A6-S1, A9-S2. Accept: each distractor has a source-backed rationale; per-distractor r_pb computed.
**A6-S3 — Forced error-classification + ONE remediation card (load-managed).** How: error-type menu (content/reasoning/trap/timing); queue ONE remediation card into FSRS (AnkiAIUtils failure-triggered field; Minimum Information Principle — no over-atomization); generated desktop-prepare. Resources: $0 (+optional LLM); 1×J/P; 1–2 days. Deps: A1-S2, A6-S1. Accept: a miss yields exactly one remediation card + error-type tag; no review-debt blowup. Binding/faithful: the engine is the feature; one card (load cap).

## A7 — Confidence capture + high-confidence-wrong router (SPOV 7) — needs A1 [DEFER]
**A7-S1 — Post-commit confidence capture (review tier).** How: template JS captures confidence AFTER commit (don't reward hedging); persist via custom-scheduling per-state customData (≤100B) or desktop reconcile (**GAP-2**). Resources: $0; 1×J; 0.5–1 day. Deps: A1-S3,S4. Accept: confidence recorded only post-commit; ≤100B/card.
**A7-S2 — High-confidence-wrong detection + false-model capture.** How: detect high-confidence+wrong; menu of candidate false models; record the pick. Resources: $0; 1×J; 0.5 day. Deps: A7-S1. Accept: HCW items flagged+tagged with a false model.
**A7-S3 — Route HCW → misconception drill (desktop-prepare).** How: desktop-prepare routes HCW → source-backed misconception drill (reuse A4/A6); NO dashboard; NO confidence-only FSRS change. Resources: $0; 1×P; 0.5–1 day. Deps: A1-S2, A7-S2. Accept: an HCW item produces a drill; confidence never alters intervals/isn't a vanity metric. Binding/faithful: confidence used ONLY to route HCW.

## A8 — Exam-date desired-retention RAMP (SPOV 11) — needs A1 [DEFER; S2b = real-Rust-change option]
**A8-S1 — Test-date input + ramp-curve math (desktop-prepare).** How: per-card DR = f(days-to-exam, yield, weakness); cap below the >97% workload cliff (MCAT consensus 88–92%); high-yield/weak first; simulate with fsrs-rs `simulate` (don't reimplement FSRS). (**GAP-5:** exact ramp curve/cap). Resources: $0 (fsrs-rs BSD-3 v6.6.1); 1×P; 1–2 days. Deps: A1-S2. Accept: a date yields a capped, prioritized per-card DR plan + projected workload.
**A8-S2 — Per-card DR write path (FORK — workaround vs native Rust). [S2b = the real Rust change]** (a) workaround (no Rust): bucket cards into per-DR decks (per-deck DR, PR #4194/25.09) or store `{"dr":92}` in customData; review order = ascending/descending retrievability off each card's DR; READ is native (PR #4880 `ctx.desired_retention`). (b) native rslib per-card-DR WRITE — implement the jhhr draft properly in the fork (**flagship real Rust change**); desktop-prepare-only until merged, removes the bucket hack. Recommended (a) now, (b) as headline change. Resources: (a) $0; (b) 1×R, 2–4 days. Deps: A1-S2, A8-S1. Accept: per-card DR applied+read on all clients (a) or natively written (b); FSRS not corrupted. Binding/faithful: a DR ramp OVER FSRS, not a cram override.
**A8-S3 — Workload cap + interval-corruption guard.** How: cap DR per yield; reschedule-on-change to pull reviews in (build on fsrs4anki-helper "Advance" = least-impact-on-retention); audit intervals vs a cram-override arm. Resources: $0; 1×P; 1 day. Deps: A8-S1,S2. Accept: ramp never exceeds cap; corruption audit passes while a cram-override arm visibly fails.

## A9 — Falsifiable metrics layer (SPOV 5) — [MVP: S1+S2 thin]
**A9-S1 — Event logging schema. [MVP]** How: log `{item, response, latency, rung, family, held-out flag}` to PostHog (free 1M events/mo) or local SQLite export; ops/UX telemetry, NOT evidence of learning. Resources: $0; 1×P; 0.5–1 day. Deps: A1-S2. Accept: events stream with all fields.
**A9-S2 — Point-biserial discrimination pipeline. [MVP]** How: corrected r_pb (exclude item from total; per-distractor for A6) via girth/psychometrics (MIT, $0); compare transfer-trained vs recall clozes (target Δr_pb ≥0.1). Resources: $0; 1×P; 1 day. Deps: A9-S1. Accept: r_pb per item; transfer-vs-cloze comparison reportable.
**A9-S3 — PFL proxy pipeline (trials-to-next-concept). [DEFER]** How: PFL = double-transfer (instruct A → new learning on novel B → score B-learning; Bransford & Schwartz; Mylopoulos d=0.62); proxy = trials-to-acquire next concept after remediation vs matched non-remediated. Resources: $0; 1×P; 1–2 days. Deps: A9-S1. Accept: trials-to-next-concept computed for remediated vs matched.
**A9-S4 — Honest reporting + held-out eval + planted-bug test. [DEFER]** How: dashboard shows discrimination+PFL; REFUSES accuracy/streaks/completion% as evidence (kept as ops); planted-broken-feature detection test. Resources: $0; 1×P; 1–2 days. Deps: A9-S2,S3. Accept: no accuracy/streak presented as proof; planted-bug test shows discrimination+PFL detect it. Binding/faithful: refuses accuracy/streaks as evidence.

## A10 — AI lane: severe-test generator + crutch kill-switch (SPOV 9) — needs A1 [DEFER]
**A10-S1 — Author the human key + provenance/trust record.** How: per concept, a key (correct solution + common mistakes) — also the PoisonedRAG defense; record provenance/trust for every AI artifact. Resources: 1×C; 0.5–1.5 hr/concept. Deps: A4-S1. Accept: every AI-touched concept has a human key + provenance.
**A10-S2 — Severe-test generation prompt (FORK — model choice).** How: prompt = enumerate assumptions → "what breaks if dropped?" → boundary/edge cases; verdict format vs key (`[[VERDICT]]`/`[[TEST]]` → consistent or a counterexample the student answers UNASSISTED); AI never final-grades (MyAnswerChecker: AI suggests, key decides). (a) GPT-4.1-mini (~$10.40/5k-mo); (b) Gemini 2.5 Flash (~$13); (c) Haiku 4.5 (~$30). Recommended (a), pick per eval. Resources: ~$10–30/mo. Deps: A1-S2, A10-S1. Accept: AI emits boundary-case items vs key; never a final grade. Binding/faithful: generate-only, never grader/body.
**A10-S3 — Prompt-injection / PoisonedRAG defense.** How: PoisonedRAG (~5 docs→~90% ASR) → provenance+isolation; validate AI output vs the human key; retrieved text never issues instructions; no auto-execution. Resources: $0; 1×P; 1 day. Deps: A10-S1,S2. Accept: a red-team doc can't make the generator emit an attacker answer that passes the key.
**A10-S4 — Legal prerequisites (OpenAI DPA + retention disclosure + GDPR).** How: sign OpenAI DPA (team=controller); disclose 30-day abuse-retention (ZDR enterprise/approval-gated); GDPR lawful basis + privacy notice + sub-processor disclosure, or geo-limit EU. Resources: 1×L + [legal]. Deps: before A10 ships to EU. Accept: DPA signed, disclosure live, lawful basis documented (or EU geo-limited). Binding/faithful (legal): no covered data to the LLM until DPA+disclosure.
**A10-S5 — Assisted/unassisted crutch A/B harness (kill-switch).** How: Bastani et al. 2025 PNAS 3-arm (Control/AI-base/guardrailed): intro → ASSISTED practice → UNASSISTED closed-book exam on matched items; pre-registered primary = unassisted; ship only if assisted−unassisted gap doesn't widen; guardrail prompt = solution+mistakes+"hints not answers"; PostHog free for flags. Resources: $0; 1×P; 2 days. Deps: A9, A10-S2. Accept: harness measures assisted vs unassisted on matched items + auto-kills a crutch-signature feature. Binding/faithful: kills assisted-up/unassisted-down scaffolds.

## A11 — CARS product decision (SPOV 12) — [MVP: decision only]
**A11-S1 — Decide build-vs-integrate (FORK — 3). [MVP]** CARS = different construct (AAMC: no specific content knowledge → recall low signal); NOT a primary deck/KPI. (a) BUILD a separate timed-passage + error-pattern module (inference/tone/scope/timing) from licensed/original or verified-free AAMC/Khan passages (**GAP-1:** verify reuse license); (b) INTEGRATE Jack Westin (BD/licensing; "no public API" UNVERIFIED → BD not self-serve); (c) DEFER CARS (science-focus; optional vocab/taxonomy micro-cards only). Resources: 1×L/PM + [legal/BD]; ~0.5 day. Deps: GAP-1. Accept: a written decision with the licensing path confirmed.
**A11-S2a (if build) — Timed-passage module + error-pattern analytics. [DEFER]** How: passage UI+timer+per-passage questions+error-pattern analytics; content licensed/verified-free/original. Resources: content licensing = binding cost; 1×J/P 3–5 days. Deps: A11-S1(a), A1-S3, A9. Accept: a timed passage runs with error-pattern analytics; no CARS deck; accuracy not the KPI.
**A11-S2b (if integrate) — Jack Westin handoff. [DEFER]** How: redirect/partnership to JW daily free + QBank (~3,044 passages); never reproduce passages. Resources: [BD/licensing]. Deps: A11-S1(b). Accept: students reach CARS via the integration; no passages scraped. Binding/faithful: not a CARS content deck/KPI.

## A12 — Non-distorting motivation layer (SPOV 8) — [MVP: thin private indicator optional]
**A12-S1 — Private mastery/coverage progress UI.** How: private indicators over A9 events; pure product/UX. Resources: $0; 1×J; 1 day. Deps: A9-S1. Accept: private mastery view; nothing public/comparative.
**A12-S2 — Recovery streaks + effort-credit for hard items.** How: recovery streaks reward RETURNING (not loss-framed unbroken streaks); effort-credit for flagged-hard/low-confidence attempts. Resources: $0; 1×J; 1 day. Deps: A9-S1. Accept: lapsing isn't punished; hard-item attempts earn credit.
**A12-S3 — Explicit exclusion guard.** How: product guardrail/lint forbidding public ranking + loss-framed streak penalties (Deci; Huang 2024 LBL-negative; UMN). Resources: $0; 0.25 day. Deps: none. Accept: a checklist/guard blocks any leaderboard/loss-streak. Binding/faithful: only non-distorting motivation.

## Critical path (load-bearing sequence)
```
A1-S1 → A1-S2 → A1-S3 → A1-S4 ─┬─► A2 (S1→S2→S3→S4)   [flagship]
                                ├─► A3 (S1∥ ; S2→S3→S4)
                                ├─► A4 (S1∥ ; S2→S3)
                                ├─► A5 / A7 (need A1-S2/S3 + estimator)
                                ├─► A8 (S1→S2→S3) [S2b = real Rust change]
                                ├─► A9 (S1→S2→S3→S4) → feeds A6-S2, A10-S5, A12
                                └─► A10 (S1→S2→S3 ; S4 legal gate ; S5 needs A9)
A1-S5 (sync) gates A1-S6 (iOS). A11 parallel (decision + GAP-1 license).
```
Content authoring (A3-S1/A4-S1/A5-S2/A6-S2/A10-S1) is the long pole — start day 1. Legal gates that block SHIPPING (not building): A1-S6/S7 (AGPL/App-Store/§13), A2-S2 (student-only), A10-S4 (DPA/GDPR), A11-S1 (content license).

## Implementation Knowledge Tree (condensed; full citations in raw-*.md + 01b)
- **A1:** one-engine-3-tiers (rslib+rsbridge → AnkiDroid JNI 3-fn → iOS C-FFI; over protobuf); custom-scheduling `SchedulingContext{deck_name,seed,+decay/DR read}` answer-time re-weight, no queue handle (SPOV 4; PR #4880 83d711f 2026-06-08); customData ≤8B keys/≤100B (rslib data.rs); AnkiConnect :8765; review = template JS (AI-Hints proof); AGPL/AnkiWeb-ToS/sync-server/no-iOS-engine-app/Play-permits-AGPL/§13.
- **A2:** UWorld-QID add-on; AAMC/UWorld non-ingestible (student-authored only); user-generated>premade d=0.45/0.29 (Pan 2023); generation ~.40 (Bertsch); BMC "all faulty"; AnkiAIUtils/MyAnswerChecker compliant patterns.
- **A3:** Chi/Feltovich/Glaser sort (surface×deep crossing); transfer conditional d=0.40 (Pan & Rickard); `tt` flag ≤100B (GAP-2 write path).
- **A4:** interleaving g=0.42 / words −0.39 / expository ns (Brunmair & Richter); Rohrer = mechanism not magnitude; AnKing hand-merge model + tag-graph; authoring $4k–18,750.
- **A5:** expertise reversal d=0.505/−0.428 (Tetzlaff 2025); backward fading + principle prompts (Renkl & Atkinson); Anki-Cloze-Interactive/Closet.
- **A6:** MC lures + feedback (Butler & Roediger); Rowland g≈0.03 no-feedback ≤50%; per-distractor point-biserial (girth); AnkiAIUtils one-card remediation.
- **A7:** JOL bias / illusion of competence (Koriat & Bjork) → no dashboard/no confidence-only FSRS.
- **A8:** READ native (PR #4880); per-deck DR (PR #4194/25.09); per-card-DR WRITE unmerged (jhhr) → workaround or native Rust change; raise-DR+Advance lever, cap 88–92%; fsrs-rs simulate.
- **A9:** corrected point-biserial; PFL double-transfer (Mylopoulos d=0.62); girth/py-irt/psychometrics $0; PostHog free.
- **A10:** assumption-drop/boundary-case prompt + `[[VERDICT]]/[[TEST]]` vs key (COUNTERMATH); Bastani PNAS +48%/−17% crutch harness; model costs $10–30/mo; OpenAI no-train/30-day/ZDR + GDPR; PoisonedRAG 5docs→90%.
- **A11:** AAMC CARS no-content; JW daily free + ~3,044 QBank, copyrighted, "no public API" UNVERIFIED (GAP-1).
- **A12:** tangible rewards undermine intrinsic motivation; LBL-negative (Huang 2024); BUT gamification net-positive on average (Sailer & Homner g≈0.49) = contested → ban mechanics not the class.

## GAPS (gap-filling pass; do not guess)
- GAP-1 (A11): Jack Westin partnership/API + Khan/AAMC CARS reuse license (BD/legal, not engineering).
- GAP-2 (A3-S4/A6-S3/A7-S1): the exact MOBILE write-back path for review-tier-captured state (per-state customData at answer-time vs deferred desktop reconcile). Reads native; non-answer-button mobile write unconfirmed.
- GAP-3 (A1-S6): AnkiMobile card-`<script>` sandbox limits (persistence, network/fetch) [verify — closed-source]. Display proven (AI-Hints); persistence/fetch not.
- GAP-4 (A5-S1/A4): a concrete per-family expertise-estimator formula (mastery threshold vs py-irt Bayesian).
- GAP-5 (A8-S1): the DR ramp curve shape + cap schedule (fsrs-rs simulate exists; the policy is unspecified).
