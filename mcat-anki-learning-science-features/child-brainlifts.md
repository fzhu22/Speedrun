# Child BrainLifts — MCAT Anki Learning-Science Features

## Lineage
- **Ancestor chain:** (none — user-initiated root run)
- **Depth:** 0
- **Sibling input (NOT a parent):** consumes `brainlifts/mcat-anki-study-app/` as high-authority, pre-gated scaffolding (its DOK 1 facts are already citation-checked; its validated insights are cited, not re-gated). This is a sibling, not a child of that run.
- **Caps:** soft depth 4, soft breadth 4 approved children/run; hard cycle/dup guard.

## Candidates (deduped; status) — child gate decision: 0 children spawned
Six CHILD BRAINLIFT CANDIDATE flags were raised across the research wave; all resolve INLINE (none spawned):

1. **"Can flashcard retrieval be engineered to build APPLICATION/transfer?"** (frontier) — this IS the parent run's core question → **cycle/dup guard: resolve inline** (it is the spine; the whole SPOV menu answers it).
2. **Motivation architecture — do streaks/points/leaderboards net-help or net-harm?** (adversary) — genuinely two-sided (Deci overjustification + streak-anxiety vs gamification meta-analyses net-positive). Big + contested, but it is ONE feature lane in this run → **resolve inline as a motivation-design SPOV** (the evidence is rich enough to forge a spiky stance without a separate full run).
3. **Implementation substrate: Anki global custom-scheduling JS vs forking the Rust scheduler (`rslib`/`fsrs-rs`)?** (cartographer) — a load-bearing engineering CONSTRAINT, not a spiky-POV topic → **resolve inline** (it shapes every feature SPOV's "how" + mobile parity).
4. **Adaptive sequencing beyond FSRS (timing vs selection/contextual-bandit on skill gain)** (edge) — feature/modeling sub-question → **inline**.
5. **Optimal scheduling + difficulty targeting for application/reasoning cards vs fact cards** (advocate) — feature/modeling sub-question → **inline**.
6. **Which MCAT question families support transfer from interleaving (surface topic vs operation vs misconception vs passage type vs AAMC skill)?** (foundations) — decides the interleaving axis → **inline**.

**Children spawned this run: 0.** All six are either the parent's own core question (cycle guard), a feature lane that becomes an inline SPOV, or an implementation constraint resolved inline. Caps untouched. (The user asked for ONE focused feature BrainLift; spawning full child runs would fragment that.)
